package com.chitter.config;

// import org.springframework.context.annotation.Configuration;
// import org.springframework.web.reactive.config.CorsRegistry;
// import org.springframework.web.reactive.config.WebFluxConfigurer;

// @Configuration
// public class SimpleCorsFilter implements WebFluxConfigurer {

//     @Override
//     public void addCorsMappings(CorsRegistry registry) {
//         registry.addMapping("/**")
//                 .allowedOrigins("http://127.0.0.1:5173")
//                 .allowedMethods("*")
//                 .allowedHeaders("*");;
//     }

// }

// import org.springframework.context.annotation.Configuration;
// import org.springframework.web.servlet.config.annotation.CorsRegistry;
// import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

// @Configuration
// public class SimpleCorsFilter implements WebMvcConfigurer {

//     @Override
//     public void addCorsMappings(CorsRegistry registry) {
//         registry.addMapping("/api/**") // Define the path you want to configure CORS for
//                 .allowedOrigins("*") // Specify allowed origins
//                 .allowedMethods("GET", "POST", "PUT", "DELETE") // Define allowed HTTP methods
//                 .allowedHeaders("*"); // Define allowed headers
//     }
// }
